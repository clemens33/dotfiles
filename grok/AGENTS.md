# Grok rules pointer

The full contract loads from `~/.claude/CLAUDE.md` through Grok's Claude compatibility.
This file exists only for Grok's 10,000-character rules cap.
Environment: macOS (Apple Silicon) or Ubuntu WSL2 (Windows host), fish shell on both.
